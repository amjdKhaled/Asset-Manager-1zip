We have now identified the exact problem from the LIVE Laserfiche API response.

The endpoint itself is CORRECT.

Swagger confirms the official endpoint is:

GET
/v1/Repositories/{repoId}/Entries/{entryId}/Laserfiche.Repository.Folder/children

The current code is calling:

/Entries/1/Laserfiche.Repository.Folder/children?$top=1000

and the live Laserfiche server returns:

HTTP 400 Bad Request

{
  "type": "invalidRequest",
  "title": "Error: A query parameter specified in the URI is not valid.",
  "status": 400
}

Therefore the problem is NOT the endpoint.

THE PROBLEM IS:

?$top=1000

This Laserfiche API endpoint on my installed version does NOT accept that query parameter.

==================================================
FIX IT NOW
==================================================

REMOVE:

?$top=1000

completely.

Use EXACTLY:

GET /v1/Repositories/{repoId}/Entries/{entryId}/Laserfiche.Repository.Folder/children

For my repository and root:

GET /v1/Repositories/TestEmployee/Entries/1/Laserfiche.Repository.Folder/children

Full request:

https://localhost/LFRepositoryAPI/v1/Repositories/TestEmployee/Entries/1/Laserfiche.Repository.Folder/children

NO:

?$top=1000

NO:

?$top=20

NO:

?$count=true

NO:

$select

NO OData query parameters at all.

==================================================
UPDATE EVERY PLACE IN THE PROJECT
==================================================

Search the ENTIRE project for every call to:

Laserfiche.Repository.Folder/children

and remove all automatically appended query parameters.

This includes:

- Dashboard service
- LaserficheEntryService
- LaserficheApiAdapter
- Probe endpoint
- Recursive folder scanner
- Any helper that builds the children URL
- Any fallback implementation

There must be ONE canonical URL builder for folder children:

/Entries/{entryId}/Laserfiche.Repository.Folder/children

Nothing after `/children`.

==================================================
ROOT ENTRY
==================================================

Continue using:

rootEntryId = 1

We already confirmed from the real Laserfiche response that:

entry.id = 1
entry.entryType = "Folder"
entry.fullPath = "\\"

Do NOT run root discovery again for this test.

==================================================
PROBE FIRST
==================================================

After changing the URL, BEFORE changing parsers or dashboard logic, run exactly:

GET
https://localhost/LFRepositoryAPI/v1/Repositories/TestEmployee/Entries/1/Laserfiche.Repository.Folder/children

Header:

Accept: application/json

Show me:

1. Exact final URL
2. HTTP status
3. Content-Type
4. COMPLETE RAW RESPONSE BODY

Do not truncate the JSON.

==================================================
IMPORTANT
==================================================

Do NOT guess another endpoint.

Do NOT change:

Laserfiche.Repository.Folder/children

The Swagger screenshot explicitly confirms this is the correct endpoint:

GET
/v1/Repositories/{repoId}/Entries/{entryId}/Laserfiche.Repository.Folder/children

The live HTTP 400 response also confirms that the route exists.

If the route did not exist we would receive 404.

We received 400 specifically because the query parameter was invalid.

So the next request MUST be the exact same endpoint WITHOUT `$top`.

==================================================
IF IT RETURNS HTTP 200
==================================================

If:

GET /Entries/1/Laserfiche.Repository.Folder/children

returns HTTP 200:

Do NOT immediately modify the response.

First inspect the exact JSON schema.

Then modify GetAllFolderChildrenAsync to deserialize THAT EXACT response structure.

After that use the returned folder IDs recursively:

/Entries/{childFolderId}/Laserfiche.Repository.Folder/children

For example:

Root 1
   ↓
GET /Entries/1/Laserfiche.Repository.Folder/children
   ↓
Folder ID 20
Folder ID 30
Document ID 40
   ↓
GET /Entries/20/Laserfiche.Repository.Folder/children
GET /Entries/30/Laserfiche.Repository.Folder/children
   ↓
Continue recursively

Only call the children endpoint for entries whose entryType is Folder.

Documents must NOT be sent to the Folder/children endpoint.

==================================================
DO NOT CLAIM THE DASHBOARD IS FIXED YET
==================================================

After making this change, stop after testing the exact endpoint.

Tell me the HTTP status and show the complete RAW JSON returned by:

/Entries/1/Laserfiche.Repository.Folder/children

We will use that response as the source of truth for the dashboard parser.