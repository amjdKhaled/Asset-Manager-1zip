We are now performing the REAL Windows validation of Task #23.

I ran the release build on the actual Windows development machine using:

cd "C:\Users\AmjadAlhawaseh\ChatBotProject\Asset-Manager-1zip"
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass -Force
.\build\publish.ps1

The script starts successfully but immediately fails after creating the
artifacts\staging directory with:

Dashboard Release Build
Version  : 1.0.0
Platform : Windows
Output   : C:\Users\AmjadAlhawaseh\ChatBotProject\Asset-Manager-1zip\artifacts

-- Cleaning previous artifacts
   [OK] artifacts\staging created.

ERROR:

The variable '$LASTEXITCODE' cannot be retrieved because it has not been set.

CategoryInfo:
InvalidOperation: (LASTEXITCODE:String) [publish.ps1], RuntimeException

FullyQualifiedErrorId:
VariableIsUndefined,publish.ps1


ROOT CAUSE TO INVESTIGATE
=========================

The script is running under PowerShell StrictMode.

$LASTEXITCODE does not necessarily exist until an external/native executable
has actually been executed.

The script appears to read $LASTEXITCODE before the first native command has
initialized it.

Do NOT simply disable StrictMode.

Do NOT hide the error.

Fix the script correctly.


REQUIRED FIX
============

Audit the ENTIRE:

build\publish.ps1

for every use of:

$LASTEXITCODE

For every occurrence, determine whether a native executable was definitely
executed immediately before it.

Never read stale or undefined $LASTEXITCODE.

Prefer a safe helper for native commands, for example a function that:

1. receives executable + arguments
2. executes the native command
3. captures the exit code immediately
4. throws a descriptive exception if the command failed
5. never depends on an old $LASTEXITCODE value

Example conceptual pattern:

function Invoke-NativeCommand {
    param(
        [Parameter(Mandatory=$true)]
        [string]$FilePath,

        [Parameter()]
        [string[]]$Arguments = @()
    )

    & $FilePath @Arguments

    $exitCode = $LASTEXITCODE

    if ($exitCode -ne 0) {
        throw "$FilePath failed with exit code $exitCode."
    }
}

You may implement a better solution if appropriate.

IMPORTANT:
Do not blindly initialize $LASTEXITCODE = 0 merely to suppress the error if
the surrounding logic is incorrect.

Fix the control flow.


FULL WINDOWS SCRIPT AUDIT
=========================

This is now the real Windows test.

Before returning the fix, inspect the ENTIRE publish.ps1 for other issues that
could fail immediately after this one.

Check especially:

- Set-StrictMode compatibility
- undefined variables
- $LASTEXITCODE usage
- $IsWindows compatibility with Windows PowerShell 5.1
- PowerShell 5.1 compatibility
- path quoting
- spaces in paths
- Join-Path usage
- Resolve-Path on paths that may not exist yet
- Test-Path before accessing optional files
- dotnet executable detection
- WiX executable detection
- wix extension installation
- net48 builds
- ASP.NET Core publish
- Desktop Extension build
- SetupHelper build
- Bootstrapper Application build
- MSI build
- Burn Bundle build
- staging directory creation
- artifact copying
- Release directory creation
- README copying
- LFDashboard-Setup.exe existence verification


DO NOT STOP AFTER FIXING LASTEXITCODE
====================================

The purpose of this task is to make:

.\build\publish.ps1

actually work on the real Windows machine.

Review the complete script proactively so we do not have to discover one
PowerShell error at a time.


IMPORTANT POWERSHELL 5.1 REQUIREMENT
====================================

The command is being run from Windows PowerShell.

Make sure the script works correctly with Windows PowerShell 5.1.

Do not rely on PowerShell 7-only features.

Keep the script compatible with:

Windows PowerShell 5.1
.NET 8 SDK
WiX v4
.NET Framework 4.8 projects


ERROR HANDLING
==============

Each build stage should produce a clear message such as:

[1/8] Publishing Dashboard Web App...
[OK] Web App published.

[2/8] Building Desktop Extension...
[OK] Desktop Extension built.

...

If a stage fails, terminate immediately with a useful error describing:

- stage
- executable
- exit code
- relevant path

Do not continue and create a misleading Release package.


FINAL ARTIFACT VERIFICATION
===========================

At the end of publish.ps1 explicitly verify:

Test-Path ".\Release\LFDashboard-Setup.exe"

and fail the build if the EXE does not exist.

Also verify README.txt.

The script must only print:

BUILD SUCCESSFUL

after both files physically exist.


DO NOT CHANGE
=============

Do not change Dashboard functionality.

Do not change Desktop/Web Client behavior.

Do not change repository detection.

Do not change login behavior.

Do not change blank-password support.

Do not reintroduce hard-coded machine-specific paths.

This task is specifically about making the Windows release/build pipeline
reliable.


AFTER FIXING
============

Report:

1. Exact root cause of the undefined LASTEXITCODE.
2. Every LASTEXITCODE occurrence found.
3. How each occurrence was fixed.
4. Any additional PowerShell bugs found.
5. Any PowerShell 5.1 incompatibilities found.
6. Every file modified.
7. The exact command I should run again on Windows.

Do NOT claim LFDashboard-Setup.exe was successfully built unless you actually
ran the full Windows pipeline in an environment capable of doing so.

Implement the fixes now.