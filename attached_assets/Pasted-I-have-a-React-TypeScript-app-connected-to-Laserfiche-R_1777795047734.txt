I have a React + TypeScript app connected to Laserfiche Repository API.

There is a problem in the Document Viewer page:
- The page shows: "Document not found"
- Error: Unexpected token '<', "<!DOCTYPE"... is not valid JSON
- No data is displayed

========================
ROOT CAUSE
========================

The frontend is calling an incorrect endpoint (likely localhost:5000 or wrong proxy),
which returns HTML instead of JSON.

========================
GOAL
========================

Fix the page so it correctly fetches and displays document data from Laserfiche
using the SAME working logic used in the Metadata section (do NOT change UI design).

========================
REQUIREMENTS
========================

1. FIX API BASE URL

- Ensure all Laserfiche API calls use:
  http://localhost/LFRepositoryAPI/v1/Repositories/{repoId}/...

- DO NOT use:
  http://localhost:5000/LFRepositoryAPI (this returns HTML)

2. FIX FETCH LOGIC

Update the document fetch to:

GET /v1/Repositories/{repoId}/Entries/{entryId}?$select=*

- Reuse the SAME function used in metadata fetching
- Do NOT create a new different logic

3. ENSURE JSON RESPONSE

Before parsing response:
- Check:
  response.headers.get("content-type")

If not "application/json":
- Throw error:
  "API is returning HTML instead of JSON (wrong endpoint)"

4. FIX FRONTEND DATA FLOW

- Use entryId passed from previous page
- Fetch:
  - Document info
  - Fields (metadata)
- Display using existing UI components

5. CLEAN METADATA VALUES

If field value is:
{ "value": null, ... }

Return:
""

6. FIX IMAGE / CONTENT FETCH (IF EXISTS)

- Ensure image or page API also uses:
  http://localhost/LFRepositoryAPI/...

7. ERROR HANDLING

- If 401 → show "Unauthorized (token issue)"
- If 404 → show "Document not found"
- If HTML → show "Wrong API endpoint configuration"

8. DEBUG LOGGING

Add console logs:
- Request URL
- Response status
- Response content-type

========================
EXPECTED RESULT
========================

- Page loads document correctly
- Metadata appears (like Document Details panel)
- No JSON parse error
- Uses same working Laserfiche logic as metadata

========================
IMPORTANT
========================

DO NOT redesign UI
ONLY fix API integration and data fetching logic
Reuse existing working code from metadata section